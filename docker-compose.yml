services:
  tests:
    build: .
    volumes:
      - ./allure-results:/app/allure-results
    environment:
      - PYTHONUNBUFFERED=1
      - HEADLESS=true   # <-- добавляем
    command: uv run pytest tests/ --alluredir=allure-results